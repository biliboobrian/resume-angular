export class Education {
    constructor(
        public name: string,
        public place: string,
        public from: Date,
        public to: Date,
        public address: string,
        public desc: string
    ) { }
}
